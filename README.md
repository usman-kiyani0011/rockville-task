

1. Install dependencies:

   ```bash
   npm install
   ```

2. Install docker and run :

   ```bash
   docker compose up
    //
    docker-compose up
   ```

3. Run the development server:

   ```bash
   npm run start:dev
   ```
